# Datenbank (PostgreSQL)
# Für lokale Entwicklung oder Neon (Cloud)
DATABASE_URL="postgresql://user:password@localhost:5432/next_dashboard"

# Auth.js v5 (Next.js 16)
# Generiere ein Secret mit: openssl rand -base64 32
AUTH_SECRET="generate_a_strong_secret_here"
AUTH_TRUST_HOST=true
# Optional: Nur wenn nötig (Vercel braucht das meist nicht)
# AUTH_URL="http://localhost:3000"

# Upstash Redis (für Rate Limiting & Account Lockout)
# Registriere dich bei https://upstash.com oder verbinde via Vercel Marketplace
UPSTASH_REDIS_REST_URL="your_upstash_redis_url"
UPSTASH_REDIS_REST_TOKEN="your_upstash_redis_token"
